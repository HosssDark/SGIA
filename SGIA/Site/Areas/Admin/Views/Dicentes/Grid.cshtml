@model IEnumerable<Repository.Repository.ViewModel.DicenteViewModel>

@{
    Layout = null;
}

<div class="row">
    @foreach (var item in Model.OrderBy(a => a.Nome))
    {
        <div class="col-xl-3 col-lg-4 col-md-6">
            <div class="card">
                <div class="card-body text-center">
                    <img class="card-profile-img" src="~/images/sm/avatar1.jpg" alt="">
                    <h5 class="mb-0">@item.Nome</h5>
                    <span>Computer</span>
                    <div class="text-muted">@item.Email</div>
                    <p class="mb-4 mt-3">@item.Status</p>
                    <p class="mb-4 mt-3">@item.DataCadastro.ToString("dd/MM/yyyy")</p>
                    <a href="@Url.Action("Detalhes", "Dicentes", new { Id = item.DicenteId })" class="btn btn-primary btn-sm">Detalhes</a>
                    <a href="@Url.Action("Alterar", "Dicentes", new { Id = item.DicenteId })" class="btn btn-primary btn-sm">Alterar</a>
                </div>
            </div>
        </div>
    }
</div>