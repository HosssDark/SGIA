@model List<Site.ViewModels.DiciplinaViewModel>

@{
    ViewData["Title"] = "Diciplinas";
}

@section scripts{
    <script type="text/javascript">
        $(document).ready(function () {
            $('.js-table').DataTable({
                responsive: true,
            });
        });
    </script>
}

<div class="card">
    <div class="header" style="background-color: #57a15f;">
        <h2 style="color: white;">@ViewData["Title"]</h2>
    </div>

    <div class="body">

        @Html.AntiForgeryToken()

        <div class="row">
            <div class="col-md-10">
                <div class="row">

                </div>
            </div>

            <div class="col-md-2">
                <a class="btn btn-primary fa-pull-right"
                   href="/Diciplinas/Adicionar">
                    Adicionar
                </a>
            </div>
        </div>

        <br />

        <div class="row">
            <div class="col-md-12">
                <div class="table">
                    <table class="table table-bordered table-striped table-hover js-table dataTable">
                        <thead>
                            <tr>
                                <th>ID</th>
                                <th>Descrição</th>
                                <th>Carga Horária</th>
                                <th>Hora Semanal</th>
                                <th>Turma</th>
                                <th>Status</th>
                                <th>Data Cadastro</th>
                                <th style="text-align: center;">Opções</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model.OrderBy(a => a.DiciplinaId))
                            {
                                <tr>
                                    <td style="vertical-align: middle !important;">@item.DiciplinaId</td>
                                    <td style="vertical-align: middle !important;">@item.Nome</td>
                                    <td style="vertical-align: middle !important;">@item.CargaHoraria</td>
                                    <td style="vertical-align: middle !important;">@item.HoraSemanal</td>
                                    <td style="vertical-align: middle !important;">@item.Turma</td>
                                    <td style="vertical-align: middle !important;">@item.Status</td>
                                    <td style="vertical-align: middle !important;">@item.DataCadastro.ToString("dd/MM/yyyy")</td>
                                    <td style="text-align: center;">
                                        <a href="/Diciplinas/Detalhes?Id=@item.DiciplinaId" class="btn btn-sm btn-primary"><i class="material-icons">search</i></a>
                                        <a href="/Diciplinas/Alterar?Id=@item.DiciplinaId" class="btn btn-sm btn-primary"><i class="material-icons">mode_edit</i></a>
                                        <a href="#" class="btn btn-sm btn-danger Excluir" data-id="@item.DiciplinaId" data-url="/Diciplinas/Excluir?Id=@item.DiciplinaId"><i class="material-icons">clear</i></a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>

    <hr />

    <div class="footer">
        <div class="col-md-12">
            <a href="/Home" class="btn btn-default fa-pull-right">Fechar</a>
        </div>
    </div>

    <br />
</div>
