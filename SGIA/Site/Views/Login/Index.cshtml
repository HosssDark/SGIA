@model Site.LoginViewModel

@{
    Layout = "~/Views/Shared/_LayoutLogin.cshtml";
    ViewData["Title"] = "Você possui conta uma conta?";
}

<div class="auth option2">
    <div class="auth_left">
        <div class="card">
            <div class="card-body">
                <div class="text-center">
                    <a class="header-brand" href="index.html"><i class="fa fa-graduation-cap brand-logo"></i></a>
                    <div class="card-title mt-3">LOGIN</div>
                </div>

                @using (Html.BeginForm("Login", "Login", FormMethod.Post))
                {
                    @Html.AntiForgeryToken()
                    @ViewData["Error"]

                    <div class="form-group">
                        @Html.LabelFor(a => a.Email)
                        @Html.TextBoxFor(a => a.Email, "", new { @class = "form-control", @required = "required", @autofocus = "autofocus" })
                        @Html.ValidationMessageFor(a => a.Email)
                    </div>
                    <div class="form-group">
                        <label class="form-label"><a href="forgot-password.html" class="float-right small">Esqueceu o seu usuário ou senha?</a></label>
                        @Html.LabelFor(a => a.Password)
                        @Html.TextBoxFor(a => a.Password, "", new { @class = "form-control", @type = "password", @required = "required" })
                        @Html.ValidationMessageFor(a => a.Password)
                    </div>

                    <div class="text-center">
                        <a href="index.html" class="btn btn-primary btn-block" title="">Acessar</a>
                        <div class="text-muted mt-4">@ViewData["Title"]<a href="@Url.Action("Cadastro","Login")"> Inscrever-se</a></div>
                    </div>
                }
            </div>
        </div>
    </div>
</div>